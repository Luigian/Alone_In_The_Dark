
usar <stddef.h> para size_t. !!!!!!!!!!!!!!!
usar <stddef.h> para NULL


usar [ * ] para malloc


max - utilizar el parametro <len> para identificar si la lista esta vacia:
if (!len)
    return (0);


rpn_cal - must not / or % by 0, and must deal with negatives operands


--------------------------------------------------------------------------------------
ALWAYS CHECK THAT EVERYTHING YOU ARE SENDING (FUNCTIONS, DATA TYPES) IS VINCULATED AND SUPPORTED. 


To backup the advances of a working exercise (not yet completed) when the exam time is out, just need to push it, not need to "grademe" it.

can_split
- sacar el total de nodos mediante una funcion recursiva que vaya, de regreso, reemplazando el value de cada nodo por el numero de hijos que tiene.
- funcion recursiva que busque si algun value (numero de hijos) es igual a ((total / 2) - 1), si lo encuentra retornar 1.